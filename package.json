{
  "name": "logisafe-cgnat",
  "version": "1.0.0",
  "private": true,
  "description": "CGNAT netmap + RADIUS mapping generator for Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "test": "node --import tsx --test test/*.test.ts",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.78.0"
  }
}
